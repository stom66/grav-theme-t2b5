{% spaceless %}

{% set jumbotron_image = page.header.hero_image ? page.media[page.header.hero_image] : page.media.images|first %}
{% set jumbotron_image_name = page.header.hero_image %}
{% set jumbotron_classes = page.header.jumbotron_classes %}

{% endspaceless %}
<div class="jumbotron {{ jumbotron_classes }}" {% if jumbotron_image %}style="background-image: url('{{ jumbotron_image.url }}');"{% endif %}>
    {{ page.header.jumbotron_content|markdown }}
</div>