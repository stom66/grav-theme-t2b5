// Import Popper + Bootstrap
import { createPopper } from '@popperjs/core';  // local Popper
import 'bootstrap';  // local Bootstrap JS which will use the local Popper internally

// Include Font Awesome JS
//import '@fortawesome/fontawesome-free/js/all.min.js';

window.onload = function () {
	console.log("window.onload()");
};
